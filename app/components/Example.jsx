var React = require('react');

var Example = React.createClass({
    render: function () {
        return (
            <div>
                <h2>Examples component</h2>
                <h4>Welcome to examples page !</h4>
            </div>
        );
    }
});

module.exports = Example;
